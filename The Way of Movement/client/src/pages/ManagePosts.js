import React from 'react';

export default function ManagePosts () {

  return (
    <div>
      <h1>Manage Posts</h1>
      {/* use blog archive to allow users to view all existing posts. add buttons to edit or delete them. as well as a button to create posts. */}
      {/* create a form for adding new posts. when form is submitted send a mutation to create the new post */}
            {/* create a form for updating posts. have form already filled with the pre-existing data */}

    </div>
  );
};
